# Article Translation System

Автоматизированный переводчик научных статей в формате LaTeX с использованием LLM (OpenAI GPT-o3/o3-mini).

## Возможности

- 🔄 Автоматический flattening LaTeX документов (`\input`, `\include`)
- 🧠 Анализ зависимостей между секциями через LLM
- 📚 Извлечение терминологии с векторной базой данных
- 🌐 Перевод с учетом контекста и зависимостей
- ✅ Автоматическая проверка сохранения формул
- 🔤 Автоматическая проверка и исправление кириллицы (для русского→английский)
- 📝 Гибкая система промптов в YAML файлах
- ⚙️ Настройка модели для каждого этапа (model, temperature, reasoning_effort)
- 📊 HTML отчеты о процессе перевода
- 🔄 Повторные попытки для проблемных участков

## Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd article-translation

# Установить зависимости
pip install -r requirements.txt

# Или установить как пакет
pip install -e .
```

## Конфигурация

Создайте файл `.env` с API ключом OpenAI:

```bash
OPENAI_API_KEY=your_api_key_here
```

Настройте `config.yaml`:

```yaml
openai:
  api_key: ${OPENAI_API_KEY}
  model: "o3-mini"  # или "o3" для более качественного перевода
  temperature: 0.3
  max_tokens: 16000

translation:
  source_language: "russian"
  target_language: "english"
  max_retries: 2
  auto_mode: true  # false для интерактивного режима

terminology:
  database_path: "terms.db"
  embedding_model: "text-embedding-3-large"
  similarity_threshold: 0.85

output:
  report_format: "html"
  mark_problematic: true
  problem_color: "red"
```

## Использование

### Из командной строки

```bash
# Базовое использование
translate-article input.tex output.tex

# С пользовательским config
translate-article input.tex output.tex --config my_config.yaml

# Интерактивный режим (для проверки терминов)
translate-article input.tex output.tex --interactive

# С кастомным путем для отчета
translate-article input.tex output.tex --report report.html
```

### Из Python кода

```python
from article_translator import ArticleTranslator

# Инициализация
translator = ArticleTranslator(config_path="config.yaml")

# Перевод
result = translator.translate(
    source_file="article.tex",
    output_file="article_en.tex",
    terminology_mode="auto"  # или "interactive"
)

# Проверка результата
if result.exit_code == 0:
    print("✓ Перевод успешно завершен")
else:
    print(f"⚠ Завершено с предупреждениями: {len(result.problematic_paragraphs)} проблемных мест")

print(f"Отчет: {result.report_path}")
print(f"Статистика: {result.statistics}")
```

## Алгоритм работы

### 1. Parsing & Flattening
- Разворачивание всех `\input` и `\include`
- Выделение секций и подсекций
- Извлечение формул (inline и display)

### 2. Dependency Analysis
- LLM анализирует связи между секциями
- Построение графа зависимостей
- Топологическая сортировка для определения порядка перевода

### 3. Terminology Extraction
- LLM извлекает специфичные термины
- Поиск похожих переводов в векторной БД
- Опциональное интерактивное уточнение
- Сохранение в базу для будущего использования

### 4. Translation
- Перевод секций в порядке зависимостей
- Контекст: переводы зависимых секций + словарь терминов
- Строгое правило: формулы остаются без изменений
- Естественный перевод текста

### 5. Formula Validation
- Извлечение формул из исходного и переведенного текста
- Сравнение на уровне абзацев:
  - **Inline формулы**: множество должно совпадать
  - **Display формулы**: порядок должен сохраняться
- До 2 повторных попыток при расхождениях
- Маркировка проблемных мест: `{\color{red}...\footnote{diff}}`

### 5.5. Cyrillic Validation (для русского)
- Автоматическая проверка на наличие кириллицы в переводе
- Поиск всех непереведенных фрагментов
- Маркировка фрагментов: `>>>текст<<<`
- Отправка на доработку с указанием конкретных мест
- Верификация исправлений

### 6. Report Generation
- HTML отчет со статистикой
- Список использованных терминов
- Детали проблемных участков
- Exit code: 0 (успех) или 1 (есть проблемы)

## Структура проекта

```
article-translation/
├── article_translator/
│   ├── __init__.py              # Публичный API
│   ├── main.py                  # Главный оркестратор
│   ├── models.py                # Dataclasses
│   ├── prompt_loader.py         # Загрузка промптов из YAML
│   ├── cyrillic_validator.py    # Валидация кириллицы
│   ├── latex_parser.py       # Парсинг LaTeX
│   ├── dependency_analyzer.py # Анализ зависимостей
│   ├── terminology_manager.py # Управление терминами
│   ├── translation_engine.py  # Движок перевода
│   ├── formula_validator.py   # Проверка формул
│   ├── report_generator.py    # Генерация отчетов
│   ├── openai_client.py       # OpenAI API клиент
│   └── cli.py                 # CLI интерфейс
├── prompts/                   # YAML файлы с промптами
│   ├── dependency_analysis.yaml
│   ├── terminology_extraction.yaml
│   ├── translation.yaml
│   ├── translation_strict_formulas.yaml
│   └── cyrillic_fix.yaml
├── examples/
│   └── sample_article.tex     # Пример статьи
├── tests/                     # Тесты (TODO)
├── config.yaml                # Конфигурация
├── requirements.txt           # Зависимости
├── setup.py                   # Установка пакета
├── DESIGN.md                  # Детальное описание
└── README.md                  # Этот файл
```

## Примеры

### Пример LaTeX статьи

См. `examples/sample_article.tex`

### Интерактивный режим

В интерактивном режиме система запросит проверку каждого термина:

```
=== Terminology Review (15 terms) ===

1/15: гильбертово пространство → Hilbert space
  Context: в теории функциональных пространств
  [a]ccept / [e]dit / [s]kip: a

2/15: собственное значение → eigenvalue
  [a]ccept / [e]dit / [s]kip: e
  New translation for 'собственное значение': proper value
```

## Коды возврата

- `0` - Успешный перевод без проблем
- `1` - Перевод завершен, но есть проблемные участки (в отчете)
- `2` - Критическая ошибка (нет API ключа, не найден файл и т.д.)

## Кастомизация промптов

Все промпты и настройки моделей хранятся в YAML файлах в директории `prompts/`.

### Структура промпт-файла

```yaml
# prompts/translation.yaml
model: "o3-mini"              # Модель для этого этапа
temperature: 0.3              # Температура сэмплирования
reasoning_effort: "high"      # low, medium, high (для o3 моделей)
max_tokens: 16000             # Максимум токенов в ответе

system_prompt: |
  System prompt с переменными {source_language}, {target_language}

user_prompt_template: |
  User prompt с переменными {content}, {dictionary}, {context}
```

### Доступные промпт-файлы

- `dependency_analysis.yaml` - анализ зависимостей между секциями
- `terminology_extraction.yaml` - извлечение терминов
- `translation.yaml` - основной перевод
- `translation_strict_formulas.yaml` - повторный перевод с акцентом на формулы
- `cyrillic_fix.yaml` - исправление непереведенных фрагментов

### Кастомизация

Вы можете редактировать эти файлы для:
- Изменения промптов под свою предметную область
- Использования разных моделей для разных этапов
- Настройки параметров reasoning_effort для o3 моделей
- Добавления специфичных инструкций

Пример использования разных моделей:
```yaml
# dependency_analysis.yaml - быстрая модель для простой задачи
model: "o3-mini"
reasoning_effort: "low"

# translation.yaml - качественная модель для важной задачи
model: "o3"
reasoning_effort: "high"
```

## Известные ограничения

- Работает только с текстовыми LaTeX файлами (не PDF)
- Требуется доступ к OpenAI API
- Может быть медленным для больших документов
- Сложные математические конструкции могут требовать ручной проверки

## Разработка

### Установка для разработки

```bash
pip install -e ".[dev]"
```

### Запуск тестов

```bash
pytest tests/
```

### Форматирование кода

```bash
black article_translator/
ruff check article_translator/
```

## Лицензия

MIT

## Авторы

Article Translation Team

## Поддержка

Для вопросов и багрепортов создавайте issue в репозитории.
